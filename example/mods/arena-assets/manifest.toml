name = "Arena Assets"
description = "Data bundle that powers the Saga example game. Registers a colour palette (palette.json) and a dimensions config (dimensions.json) under this mod's saga:// URIs during the registration phase. Other mods fetch them through the saga:assets host import."

entrypoint = "com_example_arena_assets_register"

[dependencies]
